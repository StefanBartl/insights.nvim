-- insights.nvim — plugin entry point
-- The plugin is lazy-loaded; setup() must be called by the user.
-- This file exists so that :checkhealth insights works without setup().
if vim.g.loaded_insights then return end
vim.g.loaded_insights = true
