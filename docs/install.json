{
  "tools": [
    {
      "bin": "rg",
      "required": true,
      "why": "Powers the symbol index and import-graph scanners (scan/rg.lua, symbols/rg_index.lua) — health.lua itself treats a missing rg as an error, not a warning: symbol indexing does not work at all without it.",
      "pkg": {
        "apt": "ripgrep",
        "dnf": "ripgrep",
        "pacman": "ripgrep",
        "zypper": "ripgrep",
        "apk": "ripgrep",
        "brew": "ripgrep",
        "winget": "BurntSushi.ripgrep.MSVC",
        "scoop": "ripgrep",
        "choco": "ripgrep"
      }
    },
    {
      "bin": "dot",
      "required": false,
      "why": "Lays out the import-dependency graph (imports/graph.lua) when a graph-based layout is requested, instead of the built-in layout.",
      "pkg": {
        "apt": "graphviz",
        "dnf": "graphviz",
        "pacman": "graphviz",
        "zypper": "graphviz",
        "apk": "graphviz",
        "brew": "graphviz",
        "winget": "Graphviz.Graphviz",
        "scoop": "graphviz",
        "choco": "graphviz"
      }
    }
  ]
}
